<%- include('partials/_head', {
  title: 'Cardápio Digital - Bebidas',
  description: 'Bebidas refrescantes e saborosas.',
  area: 'public'
}) %>



<%- include('partials/_navbar', { navActive: 'bebidas' }) %>

<main>
  <section class="page-header">
    <div class="container">
      <h1 class="page-title">🥤 Nossas Bebidas</h1>
      <p class="page-description">
        Bebidas refrescantes e saborosas para complementar sua refeição perfeitamente
      </p>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <div class="products-grid products-grid-full">
        <% if (bebidas && bebidas.length > 0) { %>
          <% bebidas.forEach(bebida => { %>
            <div class="product-card">
              <div class="product-image">
                <img src="<%= bebida.imagem %>" alt="<%= bebida.nome %>">
              </div>
              <div class="product-content">
                <h3 class="product-name"><%= bebida.nome %></h3>
                <p class="product-description"><%= bebida.descricao %></p>
                <div class="product-footer">
                  <span class="product-price">R$ <%= Number(bebida.preco).toFixed(2).replace('.', ',') %></span>
                </div>
              </div>
            </div>
          <% }) %>
        <% } else { %>
          <p>Nenhuma bebida cadastrada ainda.</p>
        <% } %>
      </div>
    </div>
  </section>
</main>

<%- include('partials/_footer') %>

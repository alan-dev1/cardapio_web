<%- include('../partials/_head', {
  title: '🔐 Login Admin - Cardápio Digital',
  description: 'Área administrativa - Login para gerenciar o cardápio',
  area: 'admin',
  adminPageCss: 'login.css'
}) %>

<!-- Background Shapes -->
<div class="bg-shape bg-shape-1"></div>
<div class="bg-shape bg-shape-2"></div>
<div class="bg-shape bg-shape-3"></div>

<!-- Login Container -->
<div class="login-container">
  <div class="login-card">
    <div class="login-header">
      <div class="login-icon">🔐</div>
      <h1 class="login-title">Área Administrativa</h1>
      <p class="login-subtitle">Faça login para acessar o painel de controle</p>
    </div>

    <% if (typeof erro !== 'undefined' && erro) { %>
      <div class="alert alert-error">
        <p>❌ <%= erro %></p>
      </div>
    <% } %>

    <form class="login-form" method="POST" action="/login">
      <div class="form-group">
        <label for="email" class="form-label">Email</label>
        <input
          type="email"
          id="email"
          name="email"
          class="form-input"
          placeholder="admin@exemplo.com"
          value="<%= typeof email !== 'undefined' ? email : '' %>"
          required
          autocomplete="email"
        >
      </div>

      <div class="form-group">
        <label for="senha" class="form-label">Senha</label>
        <div class="password-wrapper">
          <input
            type="password"
            id="senha"
            name="senha"
            class="form-input"
            placeholder="••••••••"
            required
            autocomplete="current-password"
          >
          <span class="password-toggle" aria-hidden="true" title="Senha">🔒</span>
        </div>
      </div>

      <div class="remember-forgot">
        <label class="remember-me">
          <input type="checkbox" id="remember" name="remember">
          <span>Lembrar-me</span>
        </label>
      </div>

      <button type="submit" class="btn-login">Entrar</button>
    </form>

    <div class="back-link">
      <a href="/registro">
        <span>📝</span>
        <span>Criar nova conta</span>
      </a>
    </div>

    <div class="back-link" style="margin-top: 0.5rem;">
      <a href="/">
        <span>←</span>
        <span>Voltar ao Cardápio</span>
      </a>
    </div>

    <div class="demo-info">
      <p><strong>👨‍💼 Conta Admin de teste:</strong></p>
      <p>Email: <code>admin@cardapio.com</code></p>
      <p>Senha: <code>admin123</code></p>
    </div>
  </div>
</div>

<style>
.alert {
  padding: 1rem;
  border-radius: 12px;
  margin-bottom: 1.5rem;
  text-align: center;
}

.alert-error {
  background: #fee2e2;
  border: 2px solid #ef4444;
  color: #991b1b;
}

.alert p {
  margin: 0;
  font-weight: 500;
}

.demo-info {
  margin-top: 2rem;
  padding: 1rem;
  background: #f0fdf4;
  border: 1px solid #86efac;
  border-radius: 12px;
  text-align: center;
  font-size: 0.85rem;
}

.demo-info p {
  margin: 0.25rem 0;
  color: #166534;
}

.demo-info code {
  background: #dcfce7;
  padding: 0.125rem 0.375rem;
  border-radius: 4px;
  font-family: 'Courier New', monospace;
  color: #15803d;
}
</style>

</body>
</html>